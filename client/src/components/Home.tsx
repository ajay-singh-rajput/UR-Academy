import React from 'react'

const Home = () => {
  return (
    <>
    <div className={`h-full p-3 flex flex-col md:flex-row relative`}>
        <div className={`md:w-1/2 `}>
            <h1 className={`flex flex-col text-7xl font-extrabold`}><span>GROW UP</span> <span className={`text-red-300 `}>YOUR SKILL</span> <span>EVERY MINUTES</span></h1>
        </div>
    </div>
    </>
  )
}

export default Home